---
// Locations hub — the four campus cards are generated from
// src/data/locations.ts so NAP details always match the location pages.
import BaseLayout from "../../layouts/BaseLayout.astro";
import { locations } from "../../data/locations";
---

<BaseLayout title="Our Locations | Imagination Grove Academy" description="Find your nearest Imagination Grove Academy campus. Four locations opening across Indiana and Kentucky &mdash; Bargersville first, with Fishers, New Palestine, and Lexington following.">

    <!-- ========== HERO ========== -->
    <section class="page-hero">
      <div class="container">
        <div class="fade-in">
          <p class="eyebrow">Our locations</p>
          <h1>Find your nearest IGA campus.</h1>
          <p>Four campuses opening across Indiana and Kentucky. Bargersville opens first, with Fishers, New Palestine, and Lexington following.</p>
        </div>
      </div>
    </section>

    <!-- ========== LOCATIONS GRID ========== -->
    <section class="section">
      <div class="container">
        <ul class="locations-grid fade-in" aria-label="IGA locations">
          {locations.map((loc) => (
            <li class={`location-card${loc.status === "open" ? " location-card--open" : ""}`}>
              <div class={`location-card-status ${loc.status === "open" ? "status-open" : "status-soon"}`}>
                <span class="status-dot" aria-hidden="true"></span>
                <span>{loc.statusLabel}</span>
              </div>
              <h2 class="location-card-city">{loc.cityState}</h2>
              {loc.areaLabel && <p class="location-card-area">{loc.areaLabel}</p>}
              <address class="location-card-address">
                {loc.streetAddress && <Fragment><span>{loc.streetAddress}</span><br /></Fragment>}
                {loc.cityState}{loc.postalCode ? ` ${loc.postalCode}` : ""}
                {loc.telephone && <Fragment><br /><span>{loc.telephone}</span></Fragment>}
              </address>
              <div class="location-card-actions">
                <a
                  href={`/locations/${loc.slug}`}
                  class={`btn ${loc.status === "open" ? "btn-green" : "btn-outline-teal"}`}
                >See this location &rarr;</a>
                <a href="/enroll" class="text-link">
                  {loc.status === "open" ? "Schedule a tour" : "Join the waitlist"} &rarr;
                </a>
              </div>
            </li>
          ))}
        </ul>
      </div>
    </section>

    <!-- ========== CTA BAND ========== -->
    <section class="cta-band section-dark">
      <div class="container">
        <div class="fade-in">
          <h2>Visit the campus near you.</h2>
          <p>Tours take about 30 minutes. See the classrooms, meet the teachers, and ask every question you have.</p>
          <a href="/enroll" class="btn btn-white btn-lg">Schedule a Free Tour</a>
        </div>
      </div>
    </section>

</BaseLayout>
